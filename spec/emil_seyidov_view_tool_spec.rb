require "spec_helper"

describe EmilSeyidovViewTool do
  it "has a version number" do
    expect(EmilSeyidovViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
